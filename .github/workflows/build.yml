name: Build ROM
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Disable git
        run: rm -rf .git

      - name: Build
        run: bash build.sh