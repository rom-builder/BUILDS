jobs:
  when:
    condition:
      not-equal: [ main, << pipeline.git.branch >> ]
  build:
    docker:
      - image: rabilrbl/aosp-build:beta
    steps:
      - checkout
      - run:
         name: Build ROM
         no_output_timeout: 24h
         command: chmod +x _build.sh && ./_build.sh